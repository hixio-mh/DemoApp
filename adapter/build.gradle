apply plugin: "com.android.library"

apply from: "${rootDir.path}/gradle/base.gradle"

android {
    resourcePrefix prefix.adapter
    defaultConfig.versionNameSuffix suffix.adapter
}

dependencies {
    implementation project(baseModule)
}