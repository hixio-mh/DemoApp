apply plugin: "com.android.library"

apply from: "${rootDir.path}/gradle/common.gradle"

android {
    resourcePrefix prefix.login
    defaultConfig.versionNameSuffix suffix.login
}

dependencies {
    implementation project(commonModule)
}